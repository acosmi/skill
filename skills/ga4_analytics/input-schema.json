{
  "type": "object",
  "properties": {
    "query": {
      "type": "string",
      "description": "查询内容、维度或指标"
    },
    "action": {
      "type": "string",
      "enum": [
        "read",
        "send",
        "search",
        "list"
      ],
      "description": "分析操作"
    },
    "env_vars": {
      "type": "object",
      "description": "所需环境变量: GA4_PROPERTY_ID, GA4_PRIVATE_KEY, SEARCH_CONSOLE_SITE_URL"
    }
  },
  "required": [
    "query"
  ]
}
