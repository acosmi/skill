{
  "type": "object",
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "search",
        "video_info",
        "playlist_list",
        "playlist_add",
        "channel_info",
        "comments",
        "captions"
      ],
      "description": "YouTube 操作"
    },
    "query": {
      "type": "string",
      "description": "搜索关键词"
    },
    "video_id": {
      "type": "string",
      "description": "视频 ID"
    },
    "playlist_id": {
      "type": "string",
      "description": "播放列表 ID"
    },
    "channel_id": {
      "type": "string",
      "description": "频道 ID"
    },
    "max_results": {
      "type": "integer",
      "description": "最大结果数，默认 10"
    },
    "order": {
      "type": "string",
      "enum": [
        "relevance",
        "date",
        "viewCount",
        "rating"
      ],
      "description": "排序方式"
    }
  },
  "required": [
    "action"
  ]
}
